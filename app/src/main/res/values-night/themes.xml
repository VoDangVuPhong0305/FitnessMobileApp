<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.FitnessMobileApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:fontFamily">@font/anton_regular</item>
        <item name="fontFamily">@font/anton_regular</item>
    </style>

    <!-- Font riêng cho chữ ở Bottom Navigation -->
    <style name="BottomNavAntonText">
        <item name="android:fontFamily">@font/anton_regular</item>
        <item name="fontFamily">@font/anton_regular</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="Theme.FitnessMobileApp" parent="Base.Theme.FitnessMobileApp" />

</resources>